@extends('layouts.saas')

@section('content')
<!-- Hero Section -->
<section class="relative pt-48 pb-32 px-6 overflow-hidden">
    <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-16 items-center">
        <div class="space-y-10 animate-reveal" id="hero-text">
            <div class="inline-flex items-center gap-3 px-4 py-2 glass-strong rounded-full text-rose-500 text-[10px] font-bold tracking-[0.2em] uppercase mb-4 glow-red">
                <span class="w-1.5 h-1.5 bg-rose-600 rounded-full animate-pulse shadow-[0_0_8px_rgba(225,29,72,0.8)]"></span> MovieShelf as a Service
            </div>
            <h1 class="text-6xl md:text-8xl font-black tracking-tighter leading-none italic">
                Dein Herz. <br><span class="text-gradient">Deine Filme.</span><br>Deine Domain.
            </h1>
            <p class="text-lg text-gray-400 max-w-lg leading-relaxed font-medium">
                Verwalte deine physischen Schätze auf deiner eigenen Plattform. Mit dedizierter Datenbank, voller Kontrolle und im preisgekrönten Cinematic-Design.
            </p>
            
            <div class="space-y-6" x-data="{ 
                subdomain: '{{ old('subdomain') }}', 
                available: {{ old('subdomain') ? 'true' : 'null' }}, 
                checking: false,
                async checkAvailability() {
                    if (this.subdomain.length < 3) {
                        this.available = null;
                        return;
                    }
                    this.checking = true;
                    try {
                        const response = await fetch(`/api/check-subdomain?name=${this.subdomain}`);
                        const data = await response.json();
                        this.available = data.available;
                        this.subdomain = data.slug;
                    } catch (e) {
                        this.available = null;
                    } finally {
                        this.checking = false;
                    }
                }
            }">
                <!-- Subdomain Claim Form -->
                @if ($errors->any())
                    <div class="px-6 py-4 bg-rose-500/20 border border-rose-500 text-rose-300 rounded-[2rem] text-sm animate-pulse mb-4 font-bold">
                        <ul>
                            @foreach ($errors->all() as $error)
                                <li><i class="bi bi-exclamation-triangle mr-2"></i>{{ $error }}</li>
                            @endforeach
                        </ul>
                    </div>
                @endif
                <form action="{{ route('tenant.register') }}" method="POST" class="space-y-6">
                    @csrf
                    <!-- Subdomain Row -->
                    <div class="glass-strong p-1.5 rounded-[2.5rem] flex flex-col lg:flex-row items-stretch lg:items-center shadow-2xl w-full max-w-5xl mx-auto group focus-within:ring-1 focus-within:ring-rose-600/50 transition-all overflow-hidden lg:h-[80px]">
                        <div class="flex-1 flex items-center px-6 py-4 lg:py-0">
                             <i class="bi bi-globe2 text-gray-500 mr-3 text-xl"></i>
                             <input type="text" 
                                    name="subdomain" 
                                    x-model="subdomain" 
                                    @input.debounce.500ms="checkAvailability()"
                                    placeholder="dein-name" 
                                    required 
                                    class="bg-transparent border-none focus:ring-0 text-white font-black w-full placeholder:text-gray-600 tracking-tight text-xl md:text-2xl uppercase italic">
                             
                             <!-- Availability Indicators -->
                             <div class="flex items-center ml-2">
                                <template x-if="checking">
                                    <i class="bi bi-arrow-repeat animate-spin text-rose-500 text-2xl"></i>
                                </template>
                                <template x-if="!checking && available === true">
                                    <i class="bi bi-check-circle-fill text-emerald-500 text-2xl glow-emerald animate-bounce"></i>
                                </template>
                                <template x-if="!checking && available === false">
                                    <i class="bi bi-x-circle-fill text-rose-600 text-2xl"></i>
                                </template>
                             </div>
                             
                             <span class="text-gray-500 font-bold hidden lg:inline ml-4 text-xl">.movieshelf.info</span>
                        </div>
                    </div>
                    
                    <!-- User Details (Revealed when available) -->
                    <div x-show="available === true" 
                         x-transition:enter="transition ease-out duration-500"
                         x-transition:enter-start="opacity-0 translate-y-10"
                         x-transition:enter-end="opacity-100 translate-y-0"
                         class="space-y-4 max-w-5xl mx-auto">
                        
                        <div class="grid md:grid-cols-2 gap-4">
                            <!-- Name -->
                            <div class="glass-strong p-4 rounded-[2rem] border border-white/5 focus-within:border-rose-600/30 transition-all">
                                <label class="text-[10px] font-bold text-gray-500 uppercase tracking-widest block mb-1 ml-2">Vollständiger Name</label>
                                <div class="flex items-center">
                                    <i class="bi bi-person text-rose-500 mr-3"></i>
                                    <input type="text" name="name" placeholder="Max Mustermann" required class="bg-transparent border-none focus:ring-0 text-white w-full font-bold">
                                </div>
                            </div>
                            
                            <!-- Email -->
                            <div class="glass-strong p-4 rounded-[2rem] border border-white/5 focus-within:border-rose-600/30 transition-all">
                                <label class="text-[10px] font-bold text-gray-500 uppercase tracking-widest block mb-1 ml-2">E-Mail Adresse</label>
                                <div class="flex items-center">
                                    <i class="bi bi-envelope text-rose-500 mr-3"></i>
                                    <input type="email" name="email" placeholder="max@beispiel.de" required class="bg-transparent border-none focus:ring-0 text-white w-full font-bold">
                                </div>
                            </div>
                        </div>

                        <div class="grid md:grid-cols-3 gap-4">
                            <!-- Username -->
                            <div class="glass-strong p-4 rounded-[2rem] border border-white/5 focus-within:border-rose-600/30 transition-all">
                                <label class="text-[10px] font-bold text-gray-500 uppercase tracking-widest block mb-1 ml-2">Benutzername</label>
                                <div class="flex items-center">
                                    <i class="bi bi-at text-rose-500 mr-3"></i>
                                    <input type="text" name="username" placeholder="max123" required class="bg-transparent border-none focus:ring-0 text-white w-full font-bold">
                                </div>
                            </div>

                            <!-- Password -->
                            <div class="glass-strong p-4 rounded-[2rem] border border-white/5 focus-within:border-rose-600/30 transition-all">
                                <label class="text-[10px] font-bold text-gray-500 uppercase tracking-widest block mb-1 ml-2">Passwort</label>
                                <div class="flex items-center">
                                    <i class="bi bi-shield-lock text-rose-500 mr-3"></i>
                                    <input type="password" name="password" placeholder="••••••••" required class="bg-transparent border-none focus:ring-0 text-white w-full font-bold">
                                </div>
                            </div>

                            <!-- Confirm Password -->
                            <div class="glass-strong p-4 rounded-[2rem] border border-white/5 focus-within:border-rose-600/30 transition-all">
                                <label class="text-[10px] font-bold text-gray-500 uppercase tracking-widest block mb-1 ml-2">Bestätigen</label>
                                <div class="flex items-center">
                                    <i class="bi bi-shield-check text-rose-500 mr-3"></i>
                                    <input type="password" name="password_confirmation" placeholder="••••••••" required class="bg-transparent border-none focus:ring-0 text-white w-full font-bold">
                                </div>
                            </div>
                        </div>

                        <!-- CTA Button -->
                        <div class="pt-4 flex justify-center">
                            <button type="submit" class="bg-rose-600 text-white px-16 py-6 rounded-full font-black italic text-xl hover:bg-rose-700 transition-all hover:scale-105 active:scale-95 shadow-2xl shadow-rose-600/40 tracking-tight">
                                JETZT MOVIESHELF STARTEN
                            </button>
                        </div>
                    </div>
                    
                    <!-- Taken Message -->
                    <p x-show="available === false" class="text-rose-500 text-[10px] font-bold uppercase tracking-widest px-8 animate-pulse text-center">
                        <i class="bi bi-exclamation-triangle mr-1"></i> Dieser Name ist leider schon vergeben.
                    </p>
                </form>
                
                <div class="flex items-center gap-4 text-[10px] font-bold uppercase tracking-widest text-gray-500 px-8 transition-opacity duration-500" :class="available === true ? 'opacity-40' : ''">
                    <div class="flex -space-x-2">
                         <img src="https://i.pravatar.cc/100?u=1" class="w-6 h-6 rounded-full border-2 border-[#0c0c0e] grayscale">
                         <img src="https://i.pravatar.cc/100?u=2" class="w-6 h-6 rounded-full border-2 border-[#0c0c0e] grayscale">
                         <img src="https://i.pravatar.cc/100?u=3" class="w-6 h-6 rounded-full border-2 border-[#0c0c0e] grayscale">
                    </div>
                    <span>Bereits 482 Sammler haben sich ihre Domain gesichert.</span>
                </div>
            </div>
        </div>
        
        <!-- Hero Mockup Side -->
        <div class="relative animate-reveal" id="hero-image">
            <div class="absolute -inset-20 bg-rose-600/5 blur-[120px] rounded-full pointer-events-none"></div>
            <div class="relative z-10 glass-strong p-2 rounded-[3.5rem] shadow-2xl overflow-hidden border border-white/5 group bg-black/20">
                <img src="/img/saas_hero.png" alt="SaaS Mockup" class="w-full h-auto rounded-[3rem] group-hover:scale-105 transition-transform duration-1000 shadow-2xl">
                <div class="absolute inset-0 bg-gradient-to-t from-[#0c0c0e]/80 via-transparent to-transparent opacity-60"></div>
            </div>
            
            <!-- Floating Badge -->
            <div class="absolute -bottom-8 -left-8 glass-strong px-8 py-5 rounded-[2rem] shadow-2xl animate-bounce delay-700 duration-1000 border border-white/5">
                <div class="flex items-center gap-5">
                    <div class="w-12 h-12 bg-rose-600/20 border border-rose-500/30 rounded-2xl flex items-center justify-center text-rose-500 text-2xl shadow-inner">
                        <i class="bi bi-shield-lock"></i>
                    </div>
                    <div>
                        <div class="text-[10px] font-bold text-gray-500 uppercase tracking-widest mb-0.5">Sicherheit</div>
                        <div class="text-sm font-black text-white italic">Eigene Datenbank</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section id="features" class="py-32 relative">
    <div class="max-w-7xl mx-auto px-6">
        <div class="text-center space-y-4 mb-24 animate-reveal">
            <h2 class="text-5xl md:text-6xl font-black italic tracking-tighter uppercase italic">Warum <span class="text-rose-600 italic">Service?</span></h2>
            <p class="text-gray-500 max-w-xl mx-auto font-medium">Vergiss Hosting, Datenbanken und Updates. Wir kümmern uns um die Technik, du dich um deine Filme.</p>
        </div>

        <div class="grid md:grid-cols-3 gap-10">
            <!-- Feature 1 -->
            <div class="animate-reveal p-12 rounded-[3rem] glass-strong hover:bg-white/[0.07] transition-all group border border-white/5 hover:border-rose-600/20">
                <div class="w-16 h-16 bg-rose-600/10 border border-rose-500/20 rounded-2xl flex items-center justify-center text-3xl text-rose-500 mb-10 group-hover:scale-110 group-hover:bg-rose-600 group-hover:text-white transition-all shadow-xl shadow-rose-600/5">
                    <i class="bi bi-database"></i>
                </div>
                <h3 class="text-2xl font-bold mb-4 italic">Volle Isolation.</h3>
                <p class="text-gray-500 leading-relaxed text-sm font-medium">
                    Keine geteilten Tabellen. Jeder User erhält eine eigene MySQL-Datenbank für maximale Sicherheit und Performance.
                </p>
            </div>

            <!-- Feature 2 -->
            <div class="animate-reveal p-12 rounded-[3rem] glass-strong hover:bg-white/[0.07] transition-all group border border-white/5 hover:border-rose-600/20">
                <div class="w-16 h-16 bg-rose-600/10 border border-rose-500/20 rounded-2xl flex items-center justify-center text-3xl text-rose-500 mb-10 group-hover:scale-110 group-hover:bg-rose-600 group-hover:text-white transition-all shadow-xl shadow-rose-600/5">
                    <i class="bi bi-magic"></i>
                </div>
                <h3 class="text-2xl font-bold mb-4 italic">Instant Setup.</h3>
                <p class="text-gray-500 leading-relaxed text-sm font-medium">
                    Keine Installation, kein FTP, kein Ärger. Registrieren, Domain wählen und sofort deine Sammlung digitalisieren.
                </p>
            </div>

            <!-- Feature 3 -->
            <div class="animate-reveal p-12 rounded-[3rem] glass-strong hover:bg-white/[0.07] transition-all group border border-white/5 hover:border-rose-600/20">
                <div class="w-16 h-16 bg-rose-600/10 border border-rose-500/20 rounded-2xl flex items-center justify-center text-3xl text-rose-500 mb-10 group-hover:scale-110 group-hover:bg-rose-600 group-hover:text-white transition-all shadow-xl shadow-rose-600/5">
                    <i class="bi bi-cloud-arrow-up"></i>
                </div>
                <h3 class="text-2xl font-bold mb-4 italic">Daily Backups.</h3>
                <p class="text-gray-500 leading-relaxed text-sm font-medium">
                    Deine Sammlung ist wertvoll. Wir sichern deine Daten jeden Tag redundant in mehreren Rechenzentren.
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Register CTA -->
<section id="register" class="py-32">
    <div class="max-w-4xl mx-auto px-6">
        <div class="relative glass-strong rounded-[4rem] p-16 md:p-24 overflow-hidden text-center space-y-12 group border-rose-600/20 border-2 shadow-[0_0_100px_-20px_rgba(225,29,72,0.15)]">
            <div class="absolute -top-20 -right-20 w-80 h-80 bg-rose-600/10 blur-[100px] rounded-full"></div>
            <div class="absolute -bottom-20 -left-20 w-80 h-80 bg-rose-900/10 blur-[100px] rounded-full"></div>
            
            <h2 class="text-5xl md:text-7xl font-black italic tracking-tighter relative z-10 leading-none">Bereit für dein <br><span class="text-gradient">Upgrade?</span></h2>
            <p class="text-gray-400 text-lg relative z-10 max-w-xl mx-auto font-medium">
                Sichere dir heute noch deinen Platz und werde Teil der exklusiven MovieShelf Community. Erhalte Zugang zu allen Cinematic-Features.
            </p>
            
            <div class="relative z-10 flex flex-col items-center gap-8">
                <button class="bg-rose-600 text-white px-16 py-6 rounded-full font-black italic text-xl hover:bg-rose-700 transition-all hover:scale-105 active:scale-95 shadow-2xl shadow-rose-600/40 tracking-tight">
                    JETZT REGISTRIEREN
                </button>
                <div class="flex items-center gap-3 text-[10px] font-bold uppercase tracking-[0.4em] text-gray-500">
                    <span class="w-12 h-px bg-gray-800"></span>
                    <span>Unverbindlich & Kostenlos</span>
                    <span class="w-12 h-px bg-gray-800"></span>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    // Reveal script is in layout, but let's fire hero immediately
    window.addEventListener('load', () => {
        document.getElementById('hero-text').classList.add('active');
        setTimeout(() => {
            document.getElementById('hero-image').classList.add('active');
        }, 300);
    });
</script>
@endsection
