<div class="animate-in fade-in slide-in-from-right-4 duration-500">    <div class="h-10 flex items-center justify-between mb-8">        <h2 class="text-xl font-black text-white flex items-center gap-4">            <div class="h-10 w-10 bg-gradient-to-br from-purple-500 to-blue-600 rounded-xl flex items-center justify-center shadow-lg shadow-purple-500/20">                <i class="bi bi-stars"></i>            </div>            <span>{{ __('Newest added') }}</span>        </h2>        <div class="flex flex-col items-end">            <span class="text-white font-black text-lg leading-none">{{ $latestMovies->count() }}</span>            <span class="text-gray-500 text-[10px] uppercase font-bold tracking-widest">{{ __('Movies') }}</span>        </div>    </div>    <div class="grid grid-cols-5 gap-2">        @forelse($latestMovies as $lat)            <div class="group cursor-pointer" @click="fetchDetails({{ $lat->id }}, '{{ $lat->backdrop_url }}')">                <div class="relative aspect-[2/3] rounded-3xl overflow-hidden glass border border-white/10 shadow-xl transition-all duration-500 group-hover:scale-[1.05] group-hover:shadow-blue-500/30 group-hover:border-blue-500/50">                    <!-- Movie Cover Placeholder -->                    <div class="absolute inset-0 bg-gray-900 flex items-center justify-center">                        @if($lat->cover_url)                            <img src="{{ $lat->cover_url }}" alt="{{ $lat->title }}" class="w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110">                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-60 group-hover:opacity-40 transition-opacity"></div>                        @else                            <i class="bi bi-film text-xl text-white/5"></i>                        @endif                    </div>                    <!-- Top Badges Flex Container -->                    <div class="absolute top-2 left-2 right-2 z-20 flex flex-wrap justify-between items-start gap-1">                        <!-- New Badge -->                        <span class="bg-blue-600 text-white text-[7px] font-black px-1.5 py-0.5 rounded shadow-lg uppercase tracking-tighter shrink-0 block">                            NEW                        </span>                        <!-- Rating Badge -->                        @if($lat->rating)                            <div class="flex items-center gap-0.5 bg-black/60 backdrop-blur-md px-1.5 py-0.5 rounded-md border border-white/10 shadow-lg shrink-0">                                <i class="bi bi-star-fill text-yellow-400 text-[8px]"></i>                                <span class="text-[9px] font-black text-white">{{ number_format($lat->rating, 1) }}</span>                            </div>                        @endif                    </div>                </div>                <div class="mt-2 px-1">                    <h3 class="text-[11px] font-black text-white leading-tight truncate group-hover:text-blue-400 transition-colors uppercase">                        {{ $lat->title }}                    </h3>                    <div class="flex items-center gap-1.5 mt-0.5 opacity-60 group-hover:opacity-100 transition-opacity">                        <span class="text-[9px] text-gray-400 font-bold italic">{{ $lat->year }}</span>                        <span class="w-0.5 h-0.5 bg-blue-500 rounded-full"></span>                        <span class="text-[9px] text-gray-400 font-bold uppercase tracking-tighter">{{ $lat->collection_type }}</span>                    </div>                </div>            </div>        @empty            <div class="col-span-full py-12 text-center glass rounded-3xl border-dashed">                <i class="bi bi-inbox text-4xl text-gray-700 mb-4 block"></i>                <p class="text-gray-500 text-sm">{{ __('No new movies found.') }}</p>            </div>        @endforelse    </div></div>