<div x-data="{         show: false,         accept() {             localStorage.setItem('cookie_consent', 'true');             this.show = false;         }     }"     x-init="if (!localStorage.getItem('cookie_consent')) { setTimeout(() => show = true, 1000) }"     x-show="show"     x-cloak     class="fixed bottom-6 right-6 z-[110] w-full max-w-sm"     x-transition:enter="transition ease-out duration-500"     x-transition:enter-start="opacity-0 translate-y-8 scale-90"     x-transition:enter-end="opacity-100 translate-y-0 scale-100"     x-transition:leave="transition ease-in duration-300"     x-transition:leave-start="opacity-100 translate-y-0 scale-100"     x-transition:leave-end="opacity-0 translate-y-8 scale-90">    <div class="glass-dark border border-white/10 rounded-3xl p-6 shadow-[0_20px_50px_-12px_rgba(0,0,0,0.5)] backdrop-blur-3xl relative overflow-hidden">        <!-- Decoration -->        <div class="absolute -top-10 -right-10 w-24 h-24 bg-cyan-500/10 rounded-full blur-3xl"></div>        <div class="flex items-start gap-4 relative z-10">            <div class="w-10 h-10 rounded-xl bg-cyan-500/20 flex items-center justify-center text-cyan-400 shrink-0">                <i class="bi bi-shield-check text-xl"></i>            </div>            <div class="flex-grow">                <h4 class="text-xs font-black text-white uppercase tracking-widest mb-2">{{ __('Privacy Update') }}</h4>                <p class="text-[11px] text-gray-400 leading-relaxed font-medium">                    {{ \App\Models\Setting::get('cookie_banner_text', 'Diese Website verwendet Cookies, um die bestmögliche Erfahrung zu bieten.') }}                </p>                <div class="mt-5 flex items-center justify-end gap-3">                    @if(\App\Models\Setting::get('impressum_enabled', '1') == '1')                    <a href="{{ route('impressum') }}" class="text-[9px] font-bold text-gray-500 uppercase tracking-widest hover:text-white transition-colors">                        {{ __('Imprint') }}                    </a>                    @endif                    <button @click="accept()" class="px-5 py-2.5 bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl text-[10px] font-black uppercase tracking-widest transition-all shadow-lg shadow-cyan-500/20 active:scale-95">                        {{ __('Verstanden') }}                    </button>                </div>            </div>        </div>    </div></div>